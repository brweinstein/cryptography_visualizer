{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "curl https://sh.rustup.rs -sSf | sh -s -- -y && export PATH=\"$HOME/.cargo/bin:$PATH\" && cargo install wasm-pack && cd ../rust && wasm-pack build --target web --out-dir ../web/public/pkg --out-name crypto_wasm && cd ../web && npm run build",
  "outputDirectory": ".next"
}
