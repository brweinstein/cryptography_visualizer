{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "curl https://sh.rustup.rs -sSf | sh -s -- -y && export PATH=\"$HOME/.cargo/bin:$PATH\" && npm run wasm:build && npm run build",
  "outputDirectory": ".next"
}
